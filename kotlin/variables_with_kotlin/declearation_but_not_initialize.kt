fun main() {
    //explicitly delcear the type of a variable ':String' / ':Int'
    var name : String 
    // if youwant just to declear a variable but not initialize it then you explicitly need to say the what is the type of variable

    name = "Toushik"
    println(name)
}
